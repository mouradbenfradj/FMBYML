<div class="fos_user_group_list">
    <ul>
        <?php var_dump(groups); die(); ?>
        {% for group in groups %}
            <li>
                <a href="{{ path('fos_user_group_show', {'groupName': group.getUserName()} ) }}">{{ group.getUSERName() }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
