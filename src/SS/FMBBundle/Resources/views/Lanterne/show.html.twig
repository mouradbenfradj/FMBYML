{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Lanterne</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Title</th>
            <td>{{ entity.title }}</td>
        </tr>
        <tr>
            <th>Lft</th>
            <td>{{ entity.lft }}</td>
        </tr>
        <tr>
            <th>Lvl</th>
            <td>{{ entity.lvl }}</td>
        </tr>
        <tr>
            <th>Rgt</th>
            <td>{{ entity.rgt }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('lanterne') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('lanterne_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
