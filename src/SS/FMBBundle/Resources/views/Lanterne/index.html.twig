{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Lanterne list</h1>

    <table class="records_list">
        <thead>
        <tr>
            <th>Id</th>
            <th>Title</th>
            <th>Lft</th>
            <th>Lvl</th>
            <th>Rgt</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lanterne_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.title }}</td>
                <td>{{ entity.lft }}</td>
                <td>{{ entity.lvl }}</td>
                <td>{{ entity.rgt }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('lanterne_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('lanterne_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lanterne_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
