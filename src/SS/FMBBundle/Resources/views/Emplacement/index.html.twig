{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Emplacement list</h1>

    <table class="records_list">
        <thead>
        <tr>
            <th>Id</th>
            <th>Place</th>
            <th>Datederemplissage</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('emplacement_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.place }}</td>
                <td>{% if entity.dateDeRemplissage %}{{ entity.dateDeRemplissage|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('emplacement_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('emplacement_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('emplacement_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
