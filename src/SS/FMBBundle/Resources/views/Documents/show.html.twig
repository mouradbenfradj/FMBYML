{% extends "SSFMBBundle::layout.html.twig" %}

{% block body -%}
    <h1>Documents</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Refdoc</th>
            <td>{{ entity.refDoc }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('documents') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('documents_edit', { 'id': entity.refDoc }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
