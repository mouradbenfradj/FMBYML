{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Filiere</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Nomfiliere</th>
            <td>{{ entity.nomFiliere }}</td>
        </tr>
        <tr>
            <th>Airedetravaille</th>
            <td>{{ entity.aireDeTravaille }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('filiere') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('filiere_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
