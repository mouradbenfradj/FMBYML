{% extends "SSFMBBundle::layout.html.twig" %}

{% block title %}
    Modifier une annonce - {{ parent() }}
{% endblock %}

{% block ssfmb_body %}
    <h1>Lot list</h1>

    <table class="records_list">
        <thead>
        <tr>
            <th>Id</th>
            <th>Lot</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('lot_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.lot }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('lot_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('lot_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lot_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
{% endblock %}
