{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Segment creation</h1>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('segment') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
